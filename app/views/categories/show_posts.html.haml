%h1
  = @category.name
- if @category_posts.empty?
  .grid_8
    %h1
      Nothing to see here. Move along please.
= will_paginate @category_posts
= render :partial => 'main/post_view', :collection => @category_posts
 