%h1
  = User.find(params[:user_id]).username + "'s Posts and Rants"

-# @posts.each do |post|
= will_paginate @posts, {:outer_window => 0, :inner_window => 1}
= render :partial => 'post', :collection => @posts