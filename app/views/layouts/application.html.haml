!!!
%html
  %head
    %title Shwagr - What's your shwagr?
    = stylesheet_link_tag :all
    -#= javascript_include_tag :defaults
    = javascript_include_tag 'http://www.google.com/jsapi'
    = javascript_include_tag 'jquery-1.4.2.min', 'application', 'chroma-hash'
    :plain
      <script type="text/javascript" charset="utf-8">
         google.load("jqueryui", "1.8");
      </script>    
    = csrf_meta_tag
  %body
    .header
      .system_messages
        - flash.each do |name,msg|
          = content_tag :div, msg, :id => "flash_#{name}", :style => "color: #fff; text-align: center; background: #666;"
      #user_nav
        - if current_user
          = link_to "#{current_user.username}", user_posts_path(current_user)
          |
          = link_to "Submit a Link", new_user_post_path(:current)
          |
          = link_to  "Edit Profile", edit_user_path(:current)
          |
          = link_to "Logout", logout_path
        - else
          = link_to "Register", new_user_path
          |
          = link_to "Login", login_path
    .container_12
      .grid_2
        = link_to 'Shwagr', root_path, :style => "font-size: 38px; font-weight: bold; margin: 20px 0; text-decoration: none;"
        %p{:style => "text-align: center;"}
          Be Realistic. Demand the Impossible.
      .main_nav.grid_10{:style => "margin-top: 35px;"}
        - for category in @categories
          %li
            = link_to "#{category.name}", show_posts_category_path(category)
      .clear
    
      = yield
